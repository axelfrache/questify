apiVersion: v1
kind: Service
metadata:
  name: {{ include "questify-gateway.fullname" . }}
  labels:
    {{- include "questify-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "questify-gateway.selectorLabels" . | nindent 4 }}
