apiVersion: v1
kind: Service
metadata:
  name: {{ include "questify-admin-service.fullname" . }}
  labels:
    {{- include "questify-admin-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "questify-admin-service.selectorLabels" . | nindent 4 }}
