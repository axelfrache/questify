apiVersion: v1
kind: Service
metadata:
  name: {{ include "questify-quest-service.fullname" . }}
  labels:
    {{- include "questify-quest-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "questify-quest-service.selectorLabels" . | nindent 4 }}
