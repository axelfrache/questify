apiVersion: v1
kind: Service
metadata:
  name: {{ include "questify-project-service.fullname" . }}
  labels:
    {{- include "questify-project-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "questify-project-service.selectorLabels" . | nindent 4 }}
