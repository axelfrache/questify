apiVersion: v1
kind: Service
metadata:
  name: {{ include "questify-frontend.fullname" . }}
  labels:
    {{- include "questify-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "questify-frontend.selectorLabels" . | nindent 4 }}
