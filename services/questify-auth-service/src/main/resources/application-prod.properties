spring.security.oauth2.resourceserver.jwt.issuer-uri=${FERRISKEY_ISSUER_URI}
questify.oidc.role-claim=${OIDC_ROLE_CLAIM:roles}
questify.oidc.allow-admin-provisioning=${OIDC_ALLOW_ADMIN_PROVISIONING:false}
questify.ferriskey.issuer-uri=${FERRISKEY_ISSUER_URI}
